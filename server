ssh <name>@ip
tmux attach  // get to the same work environment as another user, attach helps to leave the server processes when the internet goes down.
tmux detach 4 // detach from 4
cat <file name>  // list the contents of a file
