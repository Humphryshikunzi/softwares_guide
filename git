push code to different git repo to preserve commits
  git remote add CmappBackend https://github.com/Humphryshikunzi/CmappBackend.git
  git push CmappBacked master
  
  
check the origin of repo
  git remote -v

others 
 git remote
 git remote -v     // this command shows information about the uri to the github repo
 git remove -v
 git branch -vv    // check tracking branches
 git fetch origin
 git merge origin/master
 git remote add upstream <git link to original forked repo>
 git fetch upstream      // staying in sync with the original forked repo
 git merge upstream/master
 git branch -a
 git branch -r
 git log - get the commit history, identify what you committed when.
 git log -3  display the last three commits made
 git clone -b <branchname>  <url> clone a different branch other than the default
 git  log //	Lists all commits
 git reset --hard HEAD~3 //	Removes previous 3 commits, ca be 2, 4 e.t.c also check for 
 git reset --soft HEAD~3

 git config --list // list git infor for the project, and user
## install and uninstall credential manager
$ git credential-manager uninstall
$ git credential-manager install

Merging conflicts - https://www.youtube.com/watch?v=__cR7uPBOIk

 git push --set-upstream origin UdpServer -f // pushing forcefully some changes
 
 
 Delete a branch
 git branch -D <branch>
